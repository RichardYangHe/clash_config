mixed-port: 7890          # HTTP+SOCKS 混合端口
allow-lan: true           # 监听 0.0.0.0，供热点/Wi-Fi 设备使用
mode: direct              # 全局直连
log-level: info
ipv6: true

outbounds:                # ***必须存在，哪怕只有一个***
  - name: direct
    type: direct

rules:
  - MATCH,direct
